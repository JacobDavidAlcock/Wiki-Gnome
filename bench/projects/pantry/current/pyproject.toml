[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pantry-cli"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pantry = "pantry.cli:main"

[tool.setuptools]
packages = ["pantry"]
