[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tally-ledger"
version = "2.0.0"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["tally"]
